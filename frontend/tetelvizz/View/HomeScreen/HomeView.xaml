<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:tetelvizz.View.HomeScreen.Components"
             xmlns:vm="clr-namespace:tetelvizz.ViewModel"
             x:Class="tetelvizz.View.HomeView"
             BackgroundColor="#121217">
    <ContentPage.Content>
        <Grid RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- App Bar -->
            <components:CustomAppBar Grid.Row="0" />

            <!-- Scrollable Main Content -->
            <ScrollView Grid.Row="1">
                <VerticalStackLayout Padding="16" Spacing="16">
                    <components:SearchBar />
                    <components:EvaluareBacButton />
                    <components:SubjectFilter
                    SelectedSubject="{Binding SelectedSubject}"
                    IsBacSelected="{Binding IsBacSelected}" />

                    <!-- List -->
                    <!--<CollectionView ItemsSource="{Binding Files}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <components:SubjectCard ExamFile="{Binding .}" />
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView> -->
                    
                </VerticalStackLayout>
            </ScrollView>
        </Grid>
    </ContentPage.Content>
</ContentPage>
